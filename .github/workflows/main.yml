name: hello  

on:
  push:
    branches:
      - prudhvi  # Run the workflow only when changes are pushed to the "prudhvi" branch

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Print hello world
        run: echo "Hello world"
